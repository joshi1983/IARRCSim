package mapping;import graphics.*;import java.util.LinkedList;public class StopSignDetecter{  /**   Finds likely stop signs in the specified video image.  */  public static LinkedList<StopSignObstacle> findStopSigns(QuickAccessImage videoFrame)  {      // shrink the image to only 10 by 10 pixels.	  QuickAccessImage small = QuickAccessImage.shrink(videoFrame,10,10);	  // what pixel or pixels have the most red?	  	  // are they red enough to be considered as stop signs?	  LinkedList<StopSignObstacle> result = new LinkedList<StopSignObstacle>();	  	  	  	  return result;  }    }